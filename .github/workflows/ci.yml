name: CI Build
on:
  pull_request:
    branches: [master]
  push:
    branches: [master]
  workflow_dispatch:

jobs:
  build-and-test:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: Install deps
        run: |
            sudo apt update -y
            sudo apt install golang git make -y
      - name: Build
        run: make build
      - name: Test
        run: make test
  build-and-test-arm64:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - uses: uraimo/run-on-arch-action@v2
        name: Install deps and build
        id: runcmd
        with:
          arch: aarch64
          distro: ubuntu22.04
          install: |
            apt update -q -y
            apt install golang git make -q -y
            git config --global --add safe.directory /home/runner/work/event-generator/event-generator
          run: |
            make build
            make test
